<h2>New Transaction</h2>

<%= form_for @changeset, plan_account_transaction_path(@conn, :create, @account, @plan), fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Please check the errors below.</p>
    </div>
  <% end %>

  <div>
    <%= label f, :date %>
    <%= text_input f, :date %>
    <%= error_tag f, :date %>
  </div>

  <div>
    <%= label f, :merchant %>
    <%= text_input f, :merchant %>
    <%= error_tag f, :merchant %>
  </div>

  <div>
    <%= select f, :category_id, Enum.map(@categories, &{&1.name, &1.id}) %>
    <%= error_tag f, :category_id %>
  </div>

  <div class="form-group">
    <%= label f, :name, class: "control-label" %>
    <%= text_input f, :name, class: "form-control" %>
    <%= error_tag f, :name %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>

<span><%= link "Back", to: plan_path(@conn, :index) %></span>
